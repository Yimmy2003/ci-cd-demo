{
  "name": "ci-cd-demo",
  "version": "1.0.0",
  "description": "Demo de pipeline CI/CD con Jenkins y Docker",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Tests pasados correctamente\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
